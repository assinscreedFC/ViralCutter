Segment rejete. Trouve UN remplacement DIFFERENT dans le transcript.

### SEGMENT REJETE
Titre : {rejected_title}
Timestamps : {rejected_start}s - {rejected_end}s
Raison : {reject_reason}

NE REPRODUIS PAS la meme erreur. Cherche un moment COMPLETEMENT DIFFERENT.

### SEGMENTS DEJA VALIDES (NE PAS CHEVAUCHER)
{existing_segments}

### REGLES
- Timestamps DIFFERENTS du segment rejete (pas de chevauchement ±10s avec les segments valides)
- Duration : {min_duration}s-{max_duration}s
- Hook FORT dans les 3 premieres secondes
- Standalone + arc narratif complet
- start_text / end_text = mots exacts du transcript (5-10 mots)
- start_time_ref / end_time_ref = entiers (numero du tag (XXs))

### ADAPTATIONS AU TYPE DE CONTENU
{validation_rules}

TRANSCRIPT :
{transcript_chunk}

### OUTPUT (JSON ONLY)
{json_template}

JSON uniquement avec UN SEUL segment. Pas de markdown.
